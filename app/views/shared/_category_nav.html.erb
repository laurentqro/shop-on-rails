<!-- Category Sub-Navigation -->
<div class="bg-base-200 border-b border-base-300 hidden lg:block">
  <div class="container mx-auto px-4">
    <ul class="menu menu-horizontal gap-1 py-2 justify-center">
      <li>
        <%= link_to category_path('drinks'), class: "text-lg font-medium flex items-center gap-1 #{current_page?(category_path('drinks')) ? 'text-accent' : ''}" do %>
          🍹 Drinks
        <% end %>
      </li>
      <li>
        <%= link_to category_path('mains'), class: "text-lg font-medium flex items-center gap-1 #{current_page?(category_path('mains')) ? 'text-accent' : ''}" do %>
          🍔 Mains
        <% end %>
      </li>
      <li>
        <%= link_to category_path('desserts'), class: "text-lg font-medium flex items-center gap-1 #{current_page?(category_path('desserts')) ? 'text-accent' : ''}" do %>
          🍰 Desserts
        <% end %>
      </li>
      <li>
        <%= link_to category_path('bags'), class: "text-lg font-medium flex items-center gap-1 #{current_page?(category_path('bags')) ? 'text-accent' : ''}" do %>
          🛍️ Bags
        <% end %>
      </li>
      <li>
        <%= link_to category_path('accessories'), class: "text-lg font-medium flex items-center gap-1 #{current_page?(category_path('accessories')) ? 'text-accent' : ''}" do %>
          🔧 Accessories
        <% end %>
      </li>
    </ul>
  </div>
</div>
