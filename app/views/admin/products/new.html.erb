<% content_for :title, "New product" %>

<div class="w-full bg-base-100 p-5 rounded-lg">
  <h1 class="font-bold text-4xl">New product</h1>

  <%= render "form", product: @product %>

  <%= link_to "Back to products", admin_products_path, class: "btn btn-secondary" %>
</div>
