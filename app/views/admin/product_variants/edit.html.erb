<% content_for :title, "#{@product_variant.product.name} - #{@product_variant.name}" %>

<div class="w-full bg-base-100 p-5 rounded-lg">
  <h1 class="font-bold text-4xl mb-8">
    <%= @product_variant.name %> variant for <%= @product_variant.product.name %>
  </h1>

  <%= render "form", product_variant: @product_variant %>
</div>
